info:
  name: refresh-token
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/auth/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
