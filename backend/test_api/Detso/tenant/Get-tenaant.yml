info:
  name: Get-tenaant
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/tenant?search=maryadi"
  params:
    - name: search
      value: maryadi
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
