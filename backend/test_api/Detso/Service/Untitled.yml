info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/service-connection"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
